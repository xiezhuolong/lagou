<mapper namespace="product">
    <select id="selectList">
        select * from product
    </select>
    <select id="selectOne">
        select * from pruduct where id = ? and username = ?
    </select>
</mapper>